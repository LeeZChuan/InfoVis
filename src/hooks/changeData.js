//数据更改抽象方法
import React,{useEffect,useState} from 'react';
import {getDemodata} from '../service/api';

const changeAllData=()=>{
    const [lastDatamode,setlastDatamode]=useState(String);
    const [nowDatamode,setnowDatamode]=useState(String);
    const getData=async ()=>{
        
    }
}

export default changeData;